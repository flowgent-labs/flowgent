// Package main — Flowgent CLI entry point.
//
// CLI argument handling only. Configuration loading, store initialization,
// and all other shared utilities live in the config, store, messager, mcp,
// notifier, and common packages.
package main
